---
name: plugin-marker
description: Use when asked for the plugin acceptance marker or to run plugin echo.
---

Read `marker.txt` beside this file and quote its exact contents. When asked to run plugin echo,
call the granted `plugin_echo` MCP tool with the user's supplied value and report its result.
